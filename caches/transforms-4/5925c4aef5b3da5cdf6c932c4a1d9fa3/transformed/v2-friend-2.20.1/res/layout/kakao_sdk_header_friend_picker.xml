<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/title_tv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/header_margin_start"
        android:layout_marginTop="?attr/header_margin_top"
        android:layout_marginBottom="?attr/header_margin_bottom"
        android:maxLines="1"
        android:textColor="?attr/gray600s"
        android:textSize="@dimen/header_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:layout_marginBottom="@dimen/header_margin_bottom"
        tools:layout_marginTop="@dimen/header_margin_top"
        tools:text="라이언"
        tools:textColor="@color/daynight_gray600s" />

</androidx.constraintlayout.widget.ConstraintLayout>